services:
  - type: web
    name: looklyy-frontend
    env: node
    plan: starter
    rootDir: client
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NEXT_PUBLIC_API_URL
        value: https://looklyy.onrender.com
