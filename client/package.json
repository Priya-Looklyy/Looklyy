{
  "name": "lookly-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "rm -rf .next out && next build && next export",
    "start": "npx serve@latest out -p $PORT",
    "serve": "npx serve@latest out -p $PORT",
    "lint": "next lint",
    "postbuild": "echo 'Build completed successfully'"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "next": "^14.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.3.0",
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0"
  }
}
